# Client-side connection package

This is the client counterpart of the `connection` server-side package (packages/loot-core/src/platform/server/connection). This contains methods to send and listen to messages to/from the browser server.

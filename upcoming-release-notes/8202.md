---
category: Bugfix
authors: [MatissJanis]
---

Sync server: honor `ACTUAL_OPENID_ENFORCE` on the login path so a client cannot pin `loginMethod` to a dormant password/header method and bypass enforced OpenID authentication.

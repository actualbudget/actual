---
category: Bugfixes
authors: [lwarrenthompson]
---

Fix `balance_current` not syncing to API clients after bank sync. The account balance written by SimpleFIN/GoCardless was using raw SQL instead of the CRDT sync layer, so `@actual-app/api` clients never received updated bank balances.

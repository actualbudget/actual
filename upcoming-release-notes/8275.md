---
category: Maintenance
authors: [MatissJanis]
---

Reduce dependencies: replace `lodash` with `es-toolkit`, drop `ua-parser-js`, `memoize-one`, `promise-retry` and the `@juggle/resize-observer` polyfill in favour of native APIs / small in-repo helpers.

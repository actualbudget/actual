---
category: Bugfixes
authors: [helix-nine]
---

Fix `sync-server.Dockerfile` so the resulting image can resolve `@actual-app/crdt` at runtime (the workspace symlink dangled in the prod stage and the server crash-looped on startup).

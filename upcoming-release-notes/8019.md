---
category: Bugfixes
authors: [MatissJanisAboltins]
---

Fix an open redirect in OpenID login that allowed the session token to be sent to any port on the configured host (`isValidRedirectUrl` now checks the port and no longer unconditionally trusts `localhost`).

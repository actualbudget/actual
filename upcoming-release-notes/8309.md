---
category: Maintenance
authors: [MatissJanis]
---

Adopt `@total-typescript/ts-reset` to tighten unsafe built-in types (`JSON.parse` and `Response.json()` now return `unknown`, `.filter(Boolean)` narrows correctly, `Array.prototype.includes` accepts wider checks).

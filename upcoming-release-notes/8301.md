---
category: Maintenance
authors: [MatissJanis]
---

Fix the CI Lage cache so it targets Lage's real cache directory (node_modules/.cache/lage) instead of an always-empty folder, letting build/test results be reused across CI runs

---
category: Bugfixes
---

Fix **Post transaction today** for scheduled previews on mobile All Accounts, On budget, and Off budget lists (parity with single-account mobile and desktop). When saving new transactions, apply rule-driven **append/prepend notes** without overwriting notes the user already entered. Rule filter pills for payee, category, account, and similar fields show **names** instead of raw UUIDs for contains / matches / does-not-contain conditions.

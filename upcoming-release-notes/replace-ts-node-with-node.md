---
category: Maintenance
authors: [MatissJanis]
---

Run the repository's TypeScript scripts (`bin/` and loot-core pack hooks) with Node's native type stripping instead of `ts-node`, removing the `ts-node` dependency.

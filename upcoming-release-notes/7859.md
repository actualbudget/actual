---
category: Bugfixes
authors: [MatissJanis]
---

Prevent CSV formula injection in `@actual-app/cli` output and transaction CSV export by prefixing cells that start with `=`, `+`, `-`, `@`, tab, or carriage return with a single quote.

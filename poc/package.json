{
  "name": "poc-pay-periods",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --watch=false",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}

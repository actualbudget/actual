version: '3.8'

services:
  stash:
    build: .
    container_name: stash-app
    ports:
      - "3000:3000"
    volumes:
      - stash-data:/app/data
    environment:
      - NODE_ENV=production
      - STASH_PIN=${STASH_PIN:-1234}
    restart: unless-stopped

volumes:
  stash-data:

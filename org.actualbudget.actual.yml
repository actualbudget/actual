id: org.actualbudget.actual
base: org.electronjs.Electron2.BaseApp
base-version: '24.08'
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
command: actual
finish-args:
  - --share=ipc
  - --share=network
  - --device=dri
  - --socket=x11
  - --socket=wayland
  - --filesystem=home
modules:
  - name: wrapper
    buildsystem: simple
    build-commands:
      - install actual-wrapper.sh /app/bin/actual
    sources:
      - type: script
        dest-filename: actual-wrapper.sh
        commands:
          - exec zypak-wrapper /app/main/actual "$@"

  - name: actual
    buildsystem: simple
    build-commands:
      - mkdir /app/main
      - cp -R * /app/main
      - install -Dm644 resources/extra-resources/linux/org.actualbudget.actual.desktop -t /app/share/applications/
      - install -Dm644 resources/extra-resources/linux/org.actualbudget.actual.png -t /app/share/icons/hicolor/512x512/apps/
      - install -Dm644 resources/extra-resources/linux/org.actualbudget.actual.metainfo.xml -t /app/share/metainfo
    sources:
      - type: archive
        url: https://github.com/MikesGlitch/actual/releases/download/v23.9.0/linux-unpacked-x64.zip
        sha256: 588d846f1aec319f70569542ba0cb59468ca1e53abbe07a3062e3ebb9879c364
        only-arches:
          - x86_64

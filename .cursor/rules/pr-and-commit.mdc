---
description: Rules for AI-generated commits and pull requests
globs:
alwaysApply: true
---

# PR and Commit Rules for AI Agents

Canonical source: `.github/agents/pr-and-commit-rules.md`. The mechanical commit
and git rules are handled by tooling; only the rules you must apply yourself are
listed here.

## Pull Request Rules

- **ALL pull request titles MUST be prefixed with `[AI]`.** This isn't enforced
  automatically — you have to apply it yourself.
- **NEVER fill in the PR template** (`.github/PULL_REQUEST_TEMPLATE.md`) — leave it
  blank for the human. Exception: if a human explicitly asks, fill it out **in
  Chinese** (简体中文).

## GitHub comment, review and issue prefix

When you post a GitHub comment, a PR review, or create an issue / issue comment,
**prefix it with the robot emoji 🤖** so agent-authored content is visibly
marked. 🤖 must be the first character of the body (and, for issues, of the title
too). Write the text normally otherwise. (PR titles and commit messages stay
`[AI] …`.) This covers only comments **you** author; bots like CodeRabbit post
under their own identity and are unaffected. A `beforeMCPExecution` hook enforces
this — see `scripts/agent-hooks/github-comment-style.sh`.

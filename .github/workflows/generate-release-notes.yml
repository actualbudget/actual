name: Generate Release Notes

on:
  push:
    branches:
      - release/*

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate release notes
        uses: actualbudget/actions/.github/actions/notes@release/1.2.3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
